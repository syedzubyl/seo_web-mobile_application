import 'package:jaspr/dom.dart';
import 'package:jaspr/jaspr.dart';

// As your css styles are defined using just Dart, you can simply
// use global variables or methods for common things like colors.
const primaryColor = Color('#01589B');

class Screen {
  // static const mobile = MediaRule.media(maxWidth: 768.px);
  // static const tablet = MediaRule.media(minWidth: 769.px, maxWidth: 1024.px);
  // static const desktop = MediaRule.media(minWidth: 1025.px);
}
